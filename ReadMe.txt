백준 2703 문제 풀이중 입니다.

문제 풀이를 완료했습니다.

입력에서 공백을 두고 입력을 할 경우 string의 getline 함수를 사용하면 된다.
	ex) getline(cin, string 변수명);
만일 cin을 통해 숫자나 문자를 입력한 뒤 문자열을 입력하고 싶을 경우 한 번은 
cin.ignore() ( 혹은 입력을 한 번 더 받아도 된다)을 통해 입력 한 번을 무시해줄 
필요가 있다 -> 이유는 아직 찾지 못함
